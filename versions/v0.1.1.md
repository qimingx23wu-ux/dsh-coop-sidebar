# v0.1.1 · 修复渲染崩溃

> 状态：✅ 已交付（动态插件版可运行）

## 变更内容
- **修复 D1**：`useSessions()` → `useSessions(function (s) { return s })`（SnapshotSelectorHook 必须传选择器）。
- 主机端逻辑不变（词库/检测/模型解读/RPC handlers）。

## 验证
- `cordis_inspect_self`：`renderFailure` 清除，client `running`，handlers `[overview, explain]` 正常。

## 文件变更
- 动态插件定义 `coop-1/pkg-2`（进程内，未落盘）。
