# v0.2.2 · 词库扩展（68 词）+ 词库热更新机制

> 状态：✅ 代码就绪，**需重启 DSH 生效**（宿主半身变更）

## 变更内容
1. **新增 2 个分类、32 个新词条**（`glossary-extra.json`）：
   - 「项目管理」15 词：项目章程、干系人、WBS、里程碑、风险登记册、经验教训、范围蔓延、变更管理、过程组、冒烟测试、回归测试、语义化版本、MoSCoW、完成定义、埋点
   - 「开发工程」17 词：动态插件、静态插件、宿主/客户端半身、槽位、契约、RPC、JSON、Bundle、HMR、软链接、装饰器、轮询、进程/PID、持久化、降级/兜底、事件流、组合/配置声明
   - 词库总量：36 → **68**
2. **词库热更新机制**：宿主半身新增外部词库加载器（`lib/glossary-extra.json`，按文件 mtime 缓存）：
   - 内置词库（`GLOSSARY`）仍是基线；
   - 之后加词 = 编辑 `glossary-extra.json` → **5 秒内自动出现在侧边栏，无需重启、无需刷新**（overview 每 5 秒轮询）。

## 影响评估
- 改哪端：宿主 `lib/index.js`（新增加载器）→ **本次需重启 DSH 一次**；
- 重启后：加词走热更新，不再需要重启。

## 验证（已通过）
- `node --check` 语法通过；
- 冒烟：`explain('WBS')`→项目管理词条、`explain('RPC')`→开发工程词条、旧词「沙箱」仍正常；
- **热更新验证**：同一进程内修改 JSON → 立即命中新词 → 删除 → 恢复原状（文件已还原）。

## 文件变更
- 新增：`plugins/coop-sidebar/lib/glossary-extra.json`（32 词条）
- 修改：`plugins/coop-sidebar/lib/index.js`（热加载器 + mergedEntries/mergedPatterns）
- 修改：`plugins/coop-sidebar/package.json`（0.2.1 → 0.2.2）

## 重启后用户操作
1. 重启 `dsh web`；
2. 打开侧边栏「术语雷达」，本聊天中的项目管理/开发工程术语即出现（含角标计数）；
3. 之后学习新概念：编辑 `~/.dsh/profiles/web/plugins/coop-sidebar/lib/glossary-extra.json` 追加词条 → 5 秒内侧边栏自动更新。
