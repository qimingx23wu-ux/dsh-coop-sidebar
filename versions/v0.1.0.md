# v0.1.0 · 首发版本（动态插件方案）

> 状态：❌ 未正式上线（存在渲染崩溃缺陷，v0.1.1 修复）

## 交付内容
- 动态插件 `coop-1/pkg-1`（cordis_define / cordis_run 流程）。
- 右侧可收缩侧边栏三页签：术语雷达 / 过程透明 / 协作之道。
- 内置 36 词双语词库 + `llm.stream` 按需解读。
- 数据源：`sessionQuery.readSurface` 真实会话。

## 已知缺陷
- **D1（严重）**：客户端渲染崩溃 `w is not a function` —— `useSessions()` 未传选择器参数。
  → 见 `docs/03-开发过程记录.md`、`docs/04-测试与验收.md`。

## 遗留决策
- 动态插件进程本地性风险未登记 → 后续升级为静态部署（v0.2.0）。
