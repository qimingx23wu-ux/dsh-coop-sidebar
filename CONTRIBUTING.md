# 贡献指南（Contributing Guide）

感谢你愿意参与「人机协同侧边栏」的开源共建！这个项目诞生的初衷，就是帮助
**IT 小白在学习 Agent 的过程中也能看懂专业名词、信任协作过程**。你的每一份贡献都算数。

## 我能贡献什么？

| 类型 | 示例 |
|---|---|
| 🐛 提 Bug | 侧边栏遮挡、追问无响应、术语误报……请附上操作步骤与截图 |
| 💡 提需求 | 新的词库分类、交互改进、无障碍支持 |
| 📖 加词条 | 向 `plugin/lib/glossary-extra.json` 追加术语（5 段式：总结/大白话/例子/原理/边界 + 建议追问） |
| 🧪 测试 | 在不同 DSH 版本/平台验证并反馈 |
| 💻 代码 | 修复、新功能（见下方开发流程） |

## 开发流程（提交前必读）

1. **先查契约再写码**（本项目血泪教训）：
   - 平台能力用 `cordis_inspect_*` 系列查询真实签名；
   - 不确定的 API 去 DSH 源码里确认，不要凭感觉。
2. **影响三问**：这次改动 ①改哪端（宿主 `lib/index.js` / 客户端 `lib/client.js`）？
   ②要不要重启 DSH？③用户怎么拿到？（客户端→硬刷新；宿主→重启）
3. **验证**：`node --check` 两端语法 + 跑 `docs/04-测试与验收.md` 的冒烟脚本。
4. **版本与归档**：按语义化版本升 `plugin/package.json`，更新 `CHANGELOG.md`，
   在 `versions/` 新建发布说明，同步 `README.md`。
5. **提交信息**：遵循 Conventional Commits（`feat:` / `fix:` / `docs:` / `chore:`）。

## 目录结构

```
├── plugin/            # 可安装的插件源码（宿主+客户端+词库）
├── docs/              # 项目过程文档（PMP 视角：章程/需求/架构/测试/风险/经验/运维/路线图）
├── versions/          # 每个版本的发布说明
├── CHANGELOG.md       # 版本日志
└── README.md          # 项目入口
```

## 行为准则（Code of Conduct）

- 友善、就事论事；对小白提问保持耐心——**这个项目就是为小白而生的**。
- 引用事实与文档，不臆断；涉及平台行为不确定时，标注"需验证"。

> 详细开发与发布规范见 `docs/07-运维与发布手册.md`。
