# Changelog

本项目所有版本迭代记录。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)（`主.次.补丁`）。

## [v0.4.0] - 2025-08-16

### 新增
- **术语点击高亮原文（定位）**：详情面板新增「定位原文」按钮——在当前对话中查找该术语并以金色高亮环标注（最多 20 处），自动滚动到第一处，2.5 秒或滚动/缩放时清除；纯客户端、不修改主对话 DOM、无需重启。

### 说明
- 仅客户端变更 → 硬刷新生效。

## [v0.3.0] - 2025-08-16

### 新增
- **解释一键复制/导出**：详情面板新增「复制」按钮，一键复制完整 5 段式解释（复制即导出，可直接粘贴进笔记工具）；成功后按钮短暂显示「已复制 ✓」。
- 仅客户端变更 → 硬刷新生效，无需重启。

## [v0.2.3] - 2025-08-16

### 修复
- **追问无响应/跳回原解释**：模型解读失败时不再静默降级——返回 `source:'fallback'` 并携带真实失败原因，客户端显示红色警示条（"未能用模型回答你的追问（原因）。以下为词库的基础解释。"）。
- 消息构造改用官方 `createUserMessage`；宿主记录模型错误日志便于诊断。

### 说明
- 宿主 + 客户端均变更 → **本次需重启 DSH 一次**；重启后若追问仍失败，警示条会给出具体原因。

## [v0.2.2] - 2025-08-16

### 新增
- **词库热更新机制**：宿主可读取外部词库文件 `lib/glossary-extra.json`（按 mtime 缓存），之后加词无需重启，5 秒内自动出现在侧边栏。
- **词库扩展 36 → 68 词**：新增「项目管理」15 词（项目章程/干系人/WBS/里程碑/风险登记册/经验教训/范围蔓延/变更管理/过程组/冒烟测试/回归测试/语义化版本/MoSCoW/完成定义/埋点）+「开发工程」17 词（动态插件/静态插件/宿主客户端半身/槽位/契约/RPC/JSON/Bundle/HMR/软链接/装饰器/轮询/进程PID/持久化/降级兜底/事件流/组合声明）。

### 说明
- 宿主半身变更 → **本次需重启 DSH 一次**；重启后词库更新走热加载，不再需要重启。

## [v0.2.1] - 2025-08-16

### 修复
- **输入框聚焦自动收起**：在下方输入框（或任意输入区）打字时，侧边栏面板自动收起为右侧细条，不再遮挡输入；侧边栏自身的「继续追问」输入框不会误触发收起。
- 版本号对齐：插件 `package.json` 同步为 `0.2.1`。

### 说明
- 仅改客户端 bundle（`lib/client.js`），不动宿主 → **无需重启 DSH**，页面硬刷新（⌘+Shift+R）即生效。

## [v0.2.0] - 2025-08-16

### 新增（架构级变更）
- **静态部署版**：将动态插件重写为部署级静态插件 `coop-sidebar`，注册进 web profile 宿主组合。
  - 宿主半身：`TypertRemoteService` 提供 `coopSidebar` 远程服务（`overview`/`explain`），`ctx.typert.register` 注册调用描述符，纯 JS 伪造装饰器上下文实现 `@Remote` 标记。
  - 客户端半身：手写 `__ModuleLoader__.load` bundle，通过 `ctx.remote.$mount` 挂载远程命名空间，注册 `shell.overlay` 槽位。
- **持久化**：从此刷新页面、重启 DSH 均不再丢失（此前动态插件随进程重启消失）。

### 修复
- 根除「进程重启后侧边栏消失」缺陷（原动态插件方案固有局限）。

## [v0.1.1] - 2025-08-16

### 修复
- **客户端渲染崩溃（`w is not a function`）**：`useSessions` 钩子必须传选择器参数（`SnapshotSelectorHook` 签名要求），修复为 `useSessions(function (s) { return s })`。

## [v0.1.0] - 2025-08-16

### 新增（首发，动态插件方案）
- 右侧可收缩侧边栏：术语雷达（36 词内置词库 + 模型解读 + 追问）、过程透明（工具活动/权限沙箱/边界）、协作之道（人机角色与能力）。
- 数据来源：`sessionQuery.readSurface` 读真实会话；解释生成走内置词库（零成本秒出）+ `llm.stream`（未收录名词按需）。

### 已知问题（后续版本修复）
- 客户端首版渲染崩溃（v0.1.1 修复）。
- 动态插件方案不持久，进程重启即失（v0.2.0 静态化根除）。

---

## 待发布（Next）

### [v0.3.0-plan] - 提案中
- 术语点击后对话原文高亮定位
- 解释一键复制 / 导出笔记
（详见 `versions/v0.3.0-plan.md`）
